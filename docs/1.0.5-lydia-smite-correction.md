# 莉迪亚八减益补位规则：补计重击（2026-09-07）

用户确认此前八个减益不含中毒敏感度和阻挡增益。因此，“七类基础减益加中毒敏感度”的解释不适用于用户所述情况。

读取工具已经发布的只读状态后，在当前队伍的战斗快照中确认了重击：`effectTypeId=740`、`effectKindId=3021`、`effectKind=FireMark`，剩余一至二回合。该记录发生在另一轮狮子形态，不能据此证明用户此前蛇形态的八个减益包含重击。

已确认的规则缺口是：原先指定的八类减益没有包括重击。因此，七类基础减益加重击时，原规则会错误预留 A3，尽管已有八个可用减益。重击属于减益，也符合[官方效果说明](https://raid-support.plarium.com/hc/en-us/articles/360017680240-List-of-Buffs-and-Debuffs)。

## 已保存的修正

将原八类基础减益与重击组成九类候选；其中任意八类存在且各剩余至少一回合，即通过补位数量条件。降低攻击、降低防御、虚弱、降低速度和降低抗性的不同强度仍按同一类别处理。恐惧、真恐惧、中毒敏感度、阻挡增益均不参与这八类的计数。

保留蛇形态、十减益试炼、不能已有阻挡主动技能或阻挡减益、至少缺少中毒敏感度或阻挡增益等原有限制；并保留用户后来增加的“当前可进行”试炼限制。

只替换 `marius team 2` 中原有的一条莉迪亚补位规则，总数仍为 25，另外 24 条规则、队伍、目标、其他策略及当前策略选择均未改变。新修订号 `168fafa13f31f5dc`。

继续使用 1.0.5。新增条件由现有 AND/OR 表达式构成，共 57 个节点、最大深度 8，符合现有编辑器和执行器限制，无需增加规则条数或游戏命令。运行中的控制器继续使用其启动时的策略快照，修正于下次启动接管时生效。

## 验证与限制

16 项莉迪亚规则测试及离线总入口的 10 个模块通过。穷举九类候选的 512 种存在组合，并分别检查六种附加状态，共 3072 种组合。对当前保存的完整策略另外复核：八种“缺少一类基础减益、由重击补位”的情况均从原先选 A2 改为选 A3。

这确认并修复了一个规则覆盖问题。此前那次具体漏放的原因仍缺少历史状态，不能视为已经证实。规则选择 A3 也不能保证技能效果不会被抵抗。

备份、导出策略和保存回执位于 `out/marius-team-2-smite-correction`。本次只读取已有状态、离线复算及保存配置，没有提交游戏动作。

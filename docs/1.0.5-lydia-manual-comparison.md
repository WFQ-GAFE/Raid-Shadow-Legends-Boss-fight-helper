# 莉迪亚 22:30 手动成功对照排查（2026-09-07）

用户报告：约 22:30 自动没有在需要时释放莉迪亚 A3，暂停后手动释放并最终完成十减益试炼。使用的是 `build/release` 中原本的 1.0.5；此前曾连续运行一晚未完成。

手动成功证明那次手动操作抓到了有效机会。现有证据不足以还原自动判断时的完整状态，也不足以把漏放归因于某个条件。

## 已确认的事实

- 常用 EXE 原文件修改时间为 2026-09-06 04:59，不含持久决策日志模块。此前的日志更新和图标更新仅放在子目录，常用文件没有同步更新。
- 正式数据目录 `%LOCALAPPDATA%\WFQ-GAFE\RaidBossStrategyStudio` 中没有 `logs` 目录。项目内找到的决策日志是离线测试记录；12:17–12:18 的已发布战斗快照也不是本次 22:30 的记录。
- 排查时工具已关闭；两个现存游戏进程的既有只读代理决策快照和战斗流水均为空。未恢复出 22:30 的状态、规则解释、命令回执或运行中的策略副本。
- 当前保存的 `marius team 2` 为 25 条规则，修订号 `168fafa13f31f5dc`，包括此前补计重击的莉迪亚规则。恐惧和真恐惧没有被计入。
- 提取原 EXE 的实际 Python 模块，按其真实启动流程归一化当前保存的完整策略后，规则未丢失、修订号未改变。人工构造的“八类基础减益齐全”和“七类加重击”两种状态均选中 A3（47103）。原 EXE 支持当前规则，不能以版本旧为由认定它忽略了该规则。
- 接管使用启动时加载的策略快照。保存新规则不会替换已运行的控制器配置。当前保存内容不等于那晚实际加载的内容；未取得那次启动记录，不能确认是否运行了旧规则。

## 本次完成的修正

已将经过验证的日志及图标修正版复制到常用路径：

`build/release/AllianceBossStrategyStudio-1.0.5.exe`

版本保持 **1.0.5**。原 EXE 已备份至：

`build/release/archive/AllianceBossStrategyStudio-1.0.5-before-log-fix-20260907-235625.exe`

替换前确认工具未运行，先校验并备份原文件，再原子替换，最后核对新文件哈希与已验证发布包一致。本次没有改动用户策略或提交游戏动作。

下次从常用路径打开工具并启动接管后，决策记录会保存到：

`%LOCALAPPDATA%\WFQ-GAFE\RaidBossStrategyStudio\logs\decisions-*.jsonl`

记录包括实际加载的规则修订号、已观察的减益及剩余回合、试炼状态、技能冷却、已评估条件、最终选择和执行回执。暂停前已有的记录会保留；暂停后的手动操作不会由已停止的控制器自动记录。日志按容量轮换，不能恢复历史上未保存的数据。

## 验证与结论边界

最新 EXE 同样通过上述两种人工状态复算，并用包内实际 `ControllerManager` 验证决策与命令两类记录可持久写入隔离目录。该验证没有启动游戏接管。

对队伍技能和规则候选项继续核对后，尚未取得本次漏放涉及额外减益的实战证据。本次未据此猜测或扩展减益白名单。

已解决常用发布文件未同步、后续无法持久追查的问题。**22:30 那次 A3 漏放的具体原因仍未确认。** 离线通过不能作为实战正常的证明；需要下一次接管记录才能区分旧配置、条件未通过、观察数据异常或执行失败。

排查材料位于 `out/lydia-manual-comparison/`：

- `original-executable-replay.json`：原包哈希、归一化结果及人工状态复算。
- `updated-executable-replay.json`：更新包复算及包内日志写入验证。
- `release-update-receipt.json`：常用路径更新、备份及哈希校验回执。
- `replay-original-executable.py`：离线复算工具；检查历史原包时应显式传入上述备份路径，因常用路径现已更新。
